 (12 sloc)  199 Bytes
  
/**
 * main - check the code for digits 0 to 9
 * Return: Always 0.
 * @c: The character to be checked
 */
int _isdigit(int c);
{
	int i;

	for (c = 0; c <= 9; c++)
		return (1);
	else
		return (0);
